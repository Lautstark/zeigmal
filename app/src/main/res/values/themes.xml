<?xml version="1.0" encoding="utf-8"?>
<!--
  Dark, and no light theme beside it: the window is black before Compose draws
  a pixel, so a video never starts on a white flash, and the strips under the
  hidden system bars are black too. vorlaut-app's themes.xml has the longer
  argument for the dark parent on edge-to-edge Android.
-->
<resources>
    <style name="Theme.Zeigmal" parent="android:Theme.Material.NoActionBar">
        <item name="android:windowBackground">@android:color/black</item>
        <item name="android:navigationBarColor">@android:color/black</item>
        <item name="android:statusBarColor">@android:color/black</item>
    </style>
</resources>
